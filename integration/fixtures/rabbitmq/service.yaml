apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: rabbitmq-e2e
  labels:
    app: rabbitmq
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: amqp
    - name: http
      port: 15672
      targetPort: http
